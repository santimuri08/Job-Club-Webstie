<!DOCTYPE html>
<html lang="en" data-analytics-domain="{{ site.analyticsDomain }}">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="{{ description or site.description }}">
  
  <!-- Open Graph / Social -->
  <meta property="og:title" content="{{ title }} | {{ site.title }}">
  <meta property="og:description" content="{{ description or site.description }}">
  <meta property="og:type" content="website">
  
  <title>{{ title }} | {{ site.title }}</title>
  
  <!-- Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&family=Outfit:wght@600;700;800&display=swap" rel="stylesheet">
  
  <!-- Styles -->
  <link rel="stylesheet" href="/css/main.css">
</head>
<body class="intro-page">
  
  <!-- Main Content (No Header/Navbar) -->
  <main class="intro-main">
    {{ content | safe }}
  </main>

  <div class="cookie-banner" id="cookie-banner" hidden role="dialog" aria-live="polite" aria-label="Cookie preferences">
    <div class="cookie-banner-inner">
      <div class="cookie-banner-content">
        <p class="cookie-banner-title">We respect your privacy.</p>
        <p class="cookie-banner-text">We use cookies to measure site usage and improve the experience. You can accept, reject, or customize your preferences.</p>
      </div>
      <div class="cookie-banner-actions">
        <button class="btn btn-secondary" type="button" id="cookie-reject">Reject</button>
        <button class="btn btn-secondary" type="button" id="cookie-preferences">Preferences</button>
        <button class="btn btn-primary" type="button" id="cookie-accept">Accept</button>
      </div>
    </div>
  </div>

  <div class="cookie-modal" id="cookie-preferences-modal" hidden role="dialog" aria-modal="true" aria-label="Cookie preferences">
    <div class="cookie-modal-inner">
      <div class="cookie-modal-header">
        <h2 class="cookie-modal-title">Cookie Preferences</h2>
      </div>
      <div class="cookie-modal-body">
        <label class="cookie-toggle">
          <input type="checkbox" id="cookie-analytics">
          <span>Analytics cookies</span>
        </label>
        <p class="cookie-modal-text">Analytics helps us understand what’s working so we can improve the site. These cookies are optional.</p>
      </div>
      <div class="cookie-modal-actions">
        <button class="btn btn-secondary" type="button" id="cookie-preferences-cancel">Cancel</button>
        <button class="btn btn-primary" type="button" id="cookie-preferences-save">Save Preferences</button>
      </div>
    </div>
  </div>

  <script src="/js/cookie-consent.js"></script>
  <script src="/js/analytics-events.js"></script>
  
</body>
</html>