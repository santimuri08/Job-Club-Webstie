# SEO Strategy (EAiKW Harvest)

## What to look for in EAiKW

- Per-page title/description
- OpenGraph tags
- Clean URL structure and internal linking
- Performance-first design (fast TTFB, minimal JS)

## Job Club status

- `base.njk` sets title + description and basic OpenGraph tags.
- Pages use clean permalinks (`/join/`, `/events/`, `/resources/`).

## Remaining SEO TODO

- Add canonical URL tag using `site.url`.
- Add structured data for events (JSON-LD) for event detail pages.
- Add sitemap generation or a simple `sitemap.xml`.
